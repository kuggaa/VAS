#!/bin/sh

version=`VAS/build/git-version-gen $1`
echo $version
assembly_version=`echo $version | cut -d'-' -f1`

echo 🦖 🦖 🦖 Generating files for version $version ...
sed -e s/@ASSEMBLY_VERSION@/$assembly_version/g -e s/@PACKAGE_VERSION@/$version/g $2.in > $2
